<%- include('partials/header') -%>

<main class="container mt-5">
    <div class="card mt-3">
        <div class="card-header">
            <h3>Taxi for room <%=taxi.roomNumber%></h3>
        </div>
        <div class="card-body">
            <h5 class="card-title">Booking Number: <%=taxi.bookingNumber%></h5>
            <p class="card-text"><%=taxi.observations%></p>
        </div>

            <ul class="list-group list-group-flush">
                <li class="list-group-item">logged by <%=user.firstName+" "+user.lastName%></li>
                <li class="list-group-item">logged on <%=moment(taxi.createdAt).format('MMMM Do YYYY, h:mm:ss a')%></li>
                <li class="list-group-item">is it resolved? 
                    <%if(taxi.resolved){%>
                        yes
                    <%}else{%>
                        no
                    <%}%>    
                </li>
            </ul>
    </div>
    <div class="d-grid justify-content-md-end mt-5">
        <div>
            <a href="/feed" class="btn btn-outline-secondary pe-2">back to feed</a>
            <a href="/taxis/modifyTaxi/<%=taxi._id%>" class="btn btn-outline-warning">modify</a>
            <a href="/taxis/removeTaxi/<%=taxi._id%>" class="btn btn-outline-danger pe-2">remove</a>
        </div>
    </div>
</main>
<%- include('partials/footer') -%>